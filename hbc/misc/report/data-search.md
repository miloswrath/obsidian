## Key Takeaways
---
- Get NEU data sorted out with globus
- Figure out the HR data folders for UI
- Use the `zone_out.csv` for HR 
- For accelerometry: create agg function

## Accelerometer Data Search
---
- NEU Still missing most recent data after DSMC
	- Need to operationalize this
- UI Data looking good

## HR Data Search
---
- NEU Data not processed at this point
	- Missing tons of data, did not update this in February
	- If we want to include NEU data, we need to have their Globus set up correctly
- `Supervised ` and `Unsupervised` folders are not being update at this time
	- Need to determine where these files are being stored.

## HR Pipeline
---
- Has the `zone_out.csv` file which should be enough for a good plot


