## versions
Hosted deployment
local deployment

---
*global* 
- before team/programmer adjusts code to specify what is being randomzed and what variables are put into a fixed state
	- could be URL based - jp
---
*local*
- should be the same way but may use a pdf

---
### Variables
- layout generation
	- user manually builds it - no queries or wizard
	- it's revealed after the queries/wizard
	- queries/wizard result in a table and not a layout
- score timing
	- a show scores button never appears (scores are never seen)
	- button appears with the appearance of the layout/table (immediately)

### Constants
- Ready to choose button to then make the choice
- WADD from 0 - 100
- leave the reordering with the broken animation - might not be used as much without explicit instruction
- cells should have gray overlay until modified for the first time - default is gray - so users know that they have either touched them or not even if its adjusted in the wizard

### Flexible Areas 
- intro area that is easily modifiable
- help overlay with more instructions

### Data Returned 
- choice 
- they will work on a list soon

### wizard 
- options
- factors
- importance of factors
> make sure that importance affects ROW thickness not column
- how do you rate each attribute